import { Box } from "@mui/material";

const Map = () => {
    return (
        <Box component="section" sx={{ p: 2, border: '1px dashed grey' }}>
            Map
        </Box>
    );
}

export default Map
