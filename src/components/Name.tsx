import { Box } from "@mui/material";

const Name = () => {
    return (
        <Box component="section" sx={{ p: 2, border: '1px dashed grey' }}>
            Name
        </Box>
    );
}

export default Name
